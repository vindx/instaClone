import createMuiTheme from "@material-ui/core/styles/createMuiTheme";

const primaryInstaColor = createMuiTheme({
    palette: {
        primary: {
            main: '#3897F1',
        }
    }
});

export default primaryInstaColor;
